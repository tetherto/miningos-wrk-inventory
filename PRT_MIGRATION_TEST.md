# PRT migration smoke test

Temporary marker to validate the pull_request migration (github-ops PR #315):
an in-repo `feature/**` branch can be pushed and a PR opened under the new rulesets.

Safe to delete — this PR is a test and must not be merged.
